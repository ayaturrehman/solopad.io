export const metadata = {
  title: "Log In to SoloPad",
  description:
    "Log in to your SoloPad account to manage invoices, contracts, proposals, and clients — all from one dashboard.",
  alternates: { canonical: "https://www.solopad.io/login" },
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
